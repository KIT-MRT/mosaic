[project]
name = "mosaic-presentation"
version = "0.1.0"
description = "IROS 2026 lightning talk for Mosaic"
requires-python = ">=3.11"
dependencies = [
  "inkflow",
]

[tool.uv.sources]
inkflow = { git = "https://github.com/ll-nick/inkflow", branch = "main" }
